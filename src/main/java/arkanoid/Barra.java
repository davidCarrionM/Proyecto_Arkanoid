package arkanoid;

import javax.swing.JLabel;

public class Barra extends JLabel{

    Barra(Principal p){

        // derecha.setSize(43,25);
        // derecha.setLocation(430, 900);

        // centro.setSize(44,25);
        // centro.setLocation(474, 900);

        // izquierda.setSize(43,25);
        // izquierda.setLocation(517, 900);
    }
}
